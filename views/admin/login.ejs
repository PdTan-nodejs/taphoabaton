<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập - Admin</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="/css/header.css">
</head>

<body>
    <div class="login-container">
        <div class="login-box">
            <h1>Đăng nhập Admin</h1>
            <% if (error==='invalid' ) { %>
                <div class="alert alert-error">Tên đăng nhập hoặc mật khẩu không đúng!</div>
                <% } else if (error==='server' ) { %>
                    <div class="alert alert-error">Đã xảy ra lỗi. Vui lòng thử lại sau!</div>
                    <% } %>
                        <form action="/admin/login" method="POST" class="login-form">
                            <div class="form-group">
                                <label for="username">Tên đăng nhập</label>
                                <input type="text" id="username" name="username" required autofocus>
                            </div>
                            <div class="form-group">
                                <label for="password">Mật khẩu</label>
                                <input type="password" id="password" name="password" required>
                            </div>
                            <button type="submit" class="btn-primary btn-block">Đăng nhập</button>
                        </form>
                        <p class="login-note">Mặc định: username: admin, password: admin123</p>
        </div>
    </div>
</body>

</html>